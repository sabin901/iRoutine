# iRoutine Frontend — copy to .env.local and fill in values
# Required for production. Omit or use placeholder for local demo mode.

# Supabase (required for real auth and persistence)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# Supabase may also call this the publishable key.
# NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-publishable-key

# Backend API (required for energy, reflections, export, cross-domain insights)
NEXT_PUBLIC_API_URL=http://localhost:8000
